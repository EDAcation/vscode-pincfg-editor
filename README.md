# Pin Constraint Editor

This extension provides a visual editor for PCF, LPF and CST files.

It is based on [Lushay Code](https://github.com/lushaylabs/lushay-code) but strips out
the features that depend on OSS-CAD-Suite, allowing it to be used as a standalone editor
in both native and web environments.
